<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style type="text/css">
        .dotted {
            border: 1px dotted #ff0000;
            border-style: none none dotted;
            color: #fff;
            background-color: #fff;
        }

        ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

        a:link,
        a:visited {
            display: block;
            font-weight: bold;
            color: #FFFFFF;
            background-color: #98bf21;
            width: 120px;
            text-align: center;
            padding: 4px;
            text-transform: uppercase;
            text-decoration: none;
        }

        a:hover,
        a:active {
            background-color: #7A991A;
        }

        li {
            float: left;
        }

        .home {
            padding-left: 1px;
            padding-right: 1px;
            text-transform: capitalize;
        }

        body {
            background-color: white;
            margin: 0;
            padding: 0;
            border: 0;
        }

        .one {
            text-align: center;
        }

        .two {
            text-align: center;
            margin-left: 320px;
            margin-top: -16px;
        }

        .three {
            width: 636px;
            height: 250px;
            border: 2px #98bf21 solid;
            margin-left: 320px;
            margin-top: 5px;
            padding-top: 100px;
            text-align: center;
            font-size: 20px;
        }
        .four{
            font-size: 25px;
            margin-top: -50px;
        }
        .five{
            width: 100px;
            height: 25px;
        }
        .six{
            transform: translateY(40px);
            margin-left: -65px;
            width: 60px;
        }
    </style>
</head>

<body>
    <!-- <div class='main'></div> -->
    <h2 class='one'>STUDENT HOME</h2>
    <hr class='dotted' />
    <p class="home" style="margin-left: 575px;"><a href="/Home2/<%= record.email%>">Home</a></p>
    <div class='two'>
        <ul>
            <li><a href="/currentPolls/<%= record.email%>">Current Polls</a></li>
            <li><a href="/manageProfile/<%= record.email%>">Manage Profile</a></li>
            <li><a href="/viewResults/<%= record.email%>">Election Results</a></li>
            <li><a href="/electionAnalysis/<%= record.email%>">Election Analysis</a></li>
            <li><a href="/apply/<%= record.email%>">Apply Nomination</a></li>
        </ul>
    </div>
    <div class='three'>
        <form class="seven" method="POST" action="/apply/<%= record.email%>">
        <label class="four" for="position">Select position to apply</label>
        <select class="five" for="position">
            <% if(record1.length > 0) %>
            <% record1.forEach(function(data){ %>
                <option value="<%= data.name%>"><%= data.name%></option>
            <%})%>
        </select>
        <input class='six' type="submit" value="Apply" />
        </form>
    </div>
</body>

</html>